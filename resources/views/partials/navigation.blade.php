{{-- <nav class="absolute top-0 left-0 right-0 z-10">
    <div class="flex justify-between items-center w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-6">
        {{-- Logo/Nama Aplikasi --}}
        <div class="text-xl font-bold text-white">Fleur De Cake</div>
        
        {{-- Tombol Logout --}}
        {{-- <form method="POST" action="{{ route('logout') }}">
            @csrf
            <button type="submit" class="px-4 py-2 bg-white text-gray-800 rounded-full text-sm font-medium shadow-lg hover:bg-gray-100 transition duration-150">
                Logout
            </button>
        </form>
    </div>
</nav> --}}